# Core agent
google-adk>=1.0.0
google-genai>=1.0.0
python-dotenv>=1.0.1

# MCP client types used by ADK's stdio connection params
mcp<2.0.0

# WhatsApp interface (Twilio) + web layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
twilio>=9.0.0
python-multipart>=0.0.9
httpx>=0.27.0

# Shared store for multi-user linking. Only imported when TOKEN_STORE=firestore,
# but pinned here so switching backends is an env-var change, not a rebuild.
google-cloud-firestore>=2.16.0,<3.0.0
